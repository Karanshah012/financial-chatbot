# AI Financial Chatbot (LangChain + Ollama)

## Quick Start (macOS)

1. Ensure Ollama is installed and `ollama pull llama3` done.
2. Create & activate venv:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
